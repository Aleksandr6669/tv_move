rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read and write access to the 'movies' collection
    match /movies/{movieId} {
      allow read, write: if true;
    }
  }
}
